#!/bin/sh /etc/rc.common

START=99
USE_PROCD=1

start_service() {
	[ -x /usr/bin/k1-plus-wifi-ap-repair ] || return 0
	procd_open_instance
	procd_set_param command /usr/bin/k1-plus-wifi-ap-repair boot
	procd_set_param stdout 1
	procd_set_param stderr 1
	procd_close_instance
}
